//this is created to handle login but we are using the clerJS library which handles the login

/* const express = require('express');
const router = express.Router();


router.post('/',(req, res)=>{
    res.send("you are inside login page")
})

module.exports = router; */